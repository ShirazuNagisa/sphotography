1、修复后台「友链」「留言板」配置无法打开的问题：此前两个子菜单在父菜单之前注册，导致点击后跳到前台「请先创建 Fullscreen Map 页面」的错误页面。现已将友链管理与留言板设置并入「主题全局配置」主页，作为板块直接配置（不再是独立子菜单），待处理友链申请的红点提示移到「主题全局配置」主菜单上。
2、重新排版后台配置页：按大类分组并细分子板块 —— 外观(配色/明暗模式/字体与光标/卡片/日期) → 边栏与个人(站点与边栏/个人信息/页面链接栏) → 动画(基础动画/运动性格·高级) → 阅读与评论(阅读信息/评论·显示/评论·功能) → 地图(底图/标记与聚合/区域着色) → 社交(友链管理/留言板设置) → 其它(页脚/CDN) → 系统(实验性功能AI/版本与更新，固定在最下方)。实时预览独立成顶部板块，向下滚动时吸顶为常驻小预览，随时可见。右侧配置索引同步大类。
3、优化页面链接栏选项的展开动画：点击后从按钮处按文章展开页的展开逻辑 FLIP 放大成面板，面板落在页面链接栏下方；收起时缩回按钮处。
4、为友链/留言面板新增关闭逻辑：点击地图空白处可关闭；点击文章展开页中的图片、地图位移显示该图位置时，也会自动收起友链/留言面板以免遮挡。
5、修复浅色模式下评论区、友链、留言板输入框背景发黑的问题：新增 --field-bg 变量，浅色模式为白色底、深色模式维持深色底，输入文字保持深色，保证可读。
6、修复点击地图上的点或行政区上色板块时，因异步延迟偶发的重复弹出：加入点击序号守卫（作废过期的异步回调）、关闭时硬清扫残留的图片列表节点、并对同一目标的快速重复点击加入短暂锁，避免多出一个关不掉的图片列表。
