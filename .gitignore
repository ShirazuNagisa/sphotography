# WordPress
wp-content/
wp-config.php
wp-config-sample.php

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Build / Archives
*.zip
*.tar.gz
*.rar
*.7z

# Packages
node_modules/
vendor/
package-lock.json
yarn.lock

# Logs
*.log

# Documentation
/organization.md

# Geo build — large source/intermediate boundary data (keep the build script)
.geo-build/*
!.geo-build/build.mjs

# Boundary data is hosted on the `geo-data` branch (served via jsDelivr) and
# downloaded on demand into uploads/ — never shipped in the theme package or
# the master update archive.
assets/geo/