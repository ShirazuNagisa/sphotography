1、新增「配置备份」：将全部主题设置、API 密钥（明文，可选并附警告）、友链、留言板设置与地区标签颜色导出为 JSON；在新的 WordPress 上安装主题后导入即可一键恢复（覆盖式、按标签 slug 恢复地区颜色、忽略未知键、导入前二次确认、结果摘要）。
2、公告面板再次加宽「公告」标题与正文之间的间距。
3、边栏展开页瀑布流卡片整体加大；卡片大小按（摘要+标题文本长度）与有无封面连续变化，左右（三栏则左中右）列不对齐，错落有致。
4、修复个人信息栏（丰富统计面板）无法展开：内部容器 max-height:100% 在面板折叠时被夹为 0，导致高度测量为 0；改为测量内容自然高度并设定上限。
5、边栏展开按钮与页脚 GitHub 图标上下对齐（同尺寸、同右内边距）。
6、重做边栏展开页与文章展开页的衔接：二者互斥、不同时存在；点开文章时把真实文章面板迁入边栏展开页容器内部，列表左出 / 文章右入推进切换，右上角关闭再滑回列表。
7、边栏展开按钮加入鼠标吸附特效（瀑布流卡片同样支持）。
8、文章列表页搜索栏加长，填满标题与关闭按钮之间的空间。
9、修复后台设置搜索框放大镜图标与文字重叠（加大左内边距并固定图标尺寸）。
10、修复地块计数器重复计数：中国照片同时存有省与市 adcode 被分别计入，导致虚高；改为按当前上色粒度统计实际点亮的行政区（省份粒度按省、市区粒度中国按市/国外按省，与地图及饼图一致）。
