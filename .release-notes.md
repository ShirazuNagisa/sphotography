1、修复友链 / 留言面板点开后偶发变成一个圆形页面的问题：根因是收起动画以 fill 模式持续把圆角撑在 999px，下次展开便读成了圆形。现动画不再改动圆角、并在结束时正确清理动画状态。
2、修复后台「主题全局配置」页面布局错乱：所有板块恢复为整行、自上而下一行一行堆叠；实时预览改为页面顶部的普通板块（占配置索引中的一项），不再滚动吸顶；右侧配置索引保留。
3、友链 / 留言 / 照片墙三个右侧面板的右缘收窄，不再遮挡地图右上角的放大缩小 / 指南针 / 夜间模式切换按钮那一列。
4、新增「照片墙」页面：页面链接栏新增照片墙入口，点击后从按钮处按文章展开动画逻辑 FLIP 展开、收起时缩回按钮，位置大小与友链 / 留言一致且三者互斥（展开其一先收起其余）。照片墙收录所有出现在文章正文中的照片，每行五张、按拍摄日期（天）分组，每组上方按后台年月日格式标注日期；可在媒体库为照片勾选「加入照片墙置顶」，置顶照片汇入最上方的「置顶」组；采用分页无限下拉加载；上下边框带磨砂过渡。
5、照片墙照片点击后在其下方弹出三项：查看对应文章（拉出边栏并弹出对应文章、下拉到该照片位置）、查看照片详情（整页详情视图，仅展示当前照片，下方列出日期时间 / 经纬度 / 拍摄设备 / 光圈快门ISO，左右圆键按墙内顺序切换、右上角退出回到网格）、查看照片位置。
6、扩展 EXIF 采集：新增读取并存储光圈、快门、ISO 与拍摄时间；老照片可通过媒体库「重新读取 EXIF」补全；详情页有数据则显示、缺失则隐藏。
7、照片墙「查看照片位置」按当前状态分三种情况把照片坐标点平移到照片墙面板左侧空白区域的中心再缩放：A 无文章无边栏、B 仅边栏、C 文章 + 边栏（先收起文章展开页再执行 B）。照片墙面板在此过程中保持展开。
